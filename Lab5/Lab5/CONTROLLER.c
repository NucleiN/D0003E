#include <avr/io.h>
#include "TinyTimber.h"
#include "CONTROLLER.h"

int NBOB;
int SBOB;

void bridge_clear(void) {
 /*check if NBOB and SBOB is false*/
if (NBOB && SBOB){
/*return that bridge is clear and set both red lights to true*/
}

 /*if neither is true, the bridge is clear*/
 /*set both lights to red (?)*/
}